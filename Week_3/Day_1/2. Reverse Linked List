class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* t=head;
        ListNode* p=NULL;
        while (t!= NULL) {
            ListNode* f=f->next;
            t->next = p;
            p= t;
            t= f;
        }
        return p;
    }
};
