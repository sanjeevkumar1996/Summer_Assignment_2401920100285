class Solution {
public:
    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {
        ListNode dummy(0);
        ListNode* c=&dummy;
        while (list1 && list2) {
            if (list1->val < list2->val) {
                c->next = list1;
                list1=list1->next;
            } else {
                c->next=list2;
                list2=list2->next;
            }
            c=c->next;
        }
        if (list1)
            c->next = list1;
        else
            c->next = list2;
        return dummy.next;
    }
};
