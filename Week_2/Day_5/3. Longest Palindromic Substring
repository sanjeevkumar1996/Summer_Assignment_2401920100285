class Solution {
public:
    bool sol(string &s,int i,int j)
    {
        if(i>=j)
        {
            return true;
        }
        if(s[i]==s[j])
        {
            return solve(s,i+1,j-1);
        }
        return false;
    }
    string longestPalindrome(string s) {
        int n=s.size();
        int ml=INT_MIN;
        int p=0;
        for(int i=0;i<n;i++)
        {
            for(int j=i;j<n;j++)
            {
                if(sol(s,i,j)==true)
                {
                    if(j-i+1>maxlen)
                    {
                        ml=j-i+1;
                        p=i;
                    }
                }
            }
        }
        return s.substr(p,ml);
    }
};
